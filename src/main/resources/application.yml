spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: team30
  datasource:
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    url: jdbc:h2:mem:testdb;
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
  default-produces-media-type: application/json
  default-consumes-media-type: application/json
  paths-to-match:
    - /api/**
jwt:
  issuer: "team30"
  secretKey: "c3ByaW5nX2V4X3RlYW1fMzBfZmlyc3RfcHJvamVjdA=="

gemini:
  uri: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContentAIzaSyBAPbpND-AfNy3swheWJ70AoVaMRPeY0Vc
  key: AIzaSyBAPbpND-AfNy3swheWJ70AoVaMRPeY0Vc